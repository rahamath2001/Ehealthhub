import React from 'react';

const Header =() => {
    return(
        <header style={{padding:'10px',background:'RGB(35,102,123)',color:'white',textAlign:'center'}}>
            <h1>MY Header</h1>
        </header>
    );
}
export default Header;